# Reference repos cloned locally for reference only (not submodules)
refs/lingbot-csgo-finetune/

# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.ipynb_checkpoints/
.env

# TMPDIR / 分布式启动器 孤儿(没设 TMPDIR=/tmp 时留在仓库根的运行时临时目录)
tmp*/
torchelastic_*/
pymp-*/
.pymp-*/

# 运行日志(数据,不入版本库;实际产物在 outputs/,已在仓库外)
logs/

# Checkpoints / large files
*.ckpt
*.pth
*.bin
